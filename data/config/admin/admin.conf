# ZeroDayMMO Admin Configuration

admin {
  # Admin credentials
  username = "admin"
  username = ${?ZERODAY_ADMIN_USERNAME}
  
  password = "zeroday-admin-1337"
  password = ${?ZERODAY_ADMIN_PASSWORD}
  
  token = "zeroday-admin-secret-change-me"
  token = ${?ZERODAY_ADMIN_TOKEN}
  
  # Admin server settings
  port = 8081
  port = ${?ZERODAY_ADMIN_PORT}
  
  # Security settings
  rate-limit = 30
  rate-limit = ${?ZERODAY_ADMIN_RATE_LIMIT}
  
  # Log settings
  log-retention-hours = 168
  log-retention-hours = ${?ZERODAY_LOG_RETENTION_HOURS}
}